# Settlement Auditing → Revenue Certification — MOVED

This content was consolidated into the canonical reference to avoid two
overlapping files under one umbrella. See:

  references/prod-db-reconciliation-and-railway-ssh.md

That file is the single source of truth for the settlement-auditing /
revenue-certification venture: the `api/reconciliation/` engine (merged b1a786a,
deployed, inert), the classifier calibration, the V1/V2/V3 leak tests (all
falsified on GMP), the executed pivot to revenue certification, the buyer roster,
and the railway-ssh / write_file operational traps.
